# Chat Recall Web

Next.js frontend for chatrecall.ai — landing page, auth, upload, dashboard.

## Setup

```bash
pnpm install
cp .env.example .env.local
pnpm dev
```

## Testing

```bash
pnpm build
pnpm test
```
